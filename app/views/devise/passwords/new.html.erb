<div class="mt-8 mx-auto max-w-xl">
  <%= render "ui/controller_action" %>
  <div class=" bg-white border-2 p-16 text-center rounded-md">
    <div class="max-w-md mx-auto">
      <h1 class="font-display font-bold text-6xl">Forgot your password?</h1>
      <%= render "devise/shared/error_messages", resource: @user %>
      <%= form_for(
            resource,
            as: resource_name,
            url: password_path(resource_name),
            html: { method: :post, class: "mt-16 text-left mx-auto space-y-4" },
          ) do |f| %>
        <div>
          <%= f.label :email, class: "font-mono block text-sm font-medium leading-6 text-stone-900" %>
          <div class="mt-2">
            <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "you@example.com", class: "block w-full py-1.5 text-stone-900 placeholder:text-stone-400 border-2 border-stone-200 rounded-md text-sm leading-6" %>
          </div>
        </div>
        <div class="flex mx-auto">
          <%= f.submit "Email new password instructions →", class: "mt-4 w-full py-2 text-stone-50 bg-stone-800 hover:cursor-pointer rounded-md" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
